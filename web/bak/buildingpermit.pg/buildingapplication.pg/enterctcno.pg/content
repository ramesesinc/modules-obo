 <%if( PARAMS.error !=null) { %>
<pre>Error ${PARAMS.error?.message}</pre>
<% } %>


<script>

\$put("enterctcno", 
   new function() {
    var self = this;


    /*
    this.showPopup = function() {
    }


  var svc = Service.lookup("CacheService", "default");

  this.lguid;
  this.lguname;
  this.orgid = '${PARAMS.name}';
  var self = this;
  this.showPopup = function() {
    var handler = function(p) {
      self.lguid = p.id;
      self.lguname = p.name;
      self._controller.refresh();
    }

    return new PopupOpener( "openertest", {callbackHandler: handler},{width:400 , height: 500} ); 
  }
  this.submit = function() {
    window.console.log( self.lguid );
      var elem = \$("#form1");
      if (elem != null) {
        var url = "/partners/${PARAMS.name}/services/buildingpermit/buildingapplication/record";
        if (self.lguid != null) url += "?lguid=" + self.lguid;
        window.console.log( url )
        elem.attr("action", url);
        //elem.submit();
      }
    }

    //this.display = function() {
      //window.console.log("lgulist");
    //}
   */
   }
);  

\$(document).ready(function () {
      \$("input[name='radio']").click(function () {
        \$('#show-me').css('display', (\$(this).val() === '1') ? 'block':'none');

    });
       \$("input[name='radio']").click(function () {
        \$('#show-me1').css('display', (\$(this).val() === '2') ? 'block':'none');
    });
  }); 
</script>


  <div class="enterctcno-content">
    
    <h3>Enter Community Tax Certificate No. (CTC #):</h3>
    <div class="row">
      <div class="col-md-4">
        <div class="custom-radios">
        <!--<form id="form1" method="get" action="/partners/${PARAMS.name}/services/buildingpermit/buildingapplication/select" > -->
            
          <label class="control-label" style="padding-right:20px;">
            <input type="radio" name="radio" value="1" checked>
              Resident
          </label>
          <label class="control-label">
            <input type="radio" name="radio" value="2" >
               Non-resident
          </label>
          <div class="control-group" id='show-me' >
            <div class="inputSelected">
              <label class="control-label">CTC No.</label>
              <input type="text" r:context="app" r:name="ctcno" r:required="true" class="form-control"/>
            </div>
          </div>
          <div class="control-group" id='show-me1' style='display:none'>
            <div class="inputSelected">
            <label class="control-label">CTC No.</label>
            <input type="text" r:context="app" r:name="ctcno" r:required="true" class="form-control"/>
            <label class="control-label">Search LGU</label>
              <div class="input-group">
                <input type="text" r:context="app" r:name="lguname" name="lguname" class="form-control"/>
                <span class="input-group-btn">
                  <button r:context="app" r:name="showPopup" class="btn btn-popup"><i class="fa fa-search"></i></button>
                </span>
              </div>
            </div>
          </div>
          
          <!--<div class="row" style="margin-top:30px;">
            <div class="col-md-4">
              <input class="thumbnail btn btn-primary" type="submit" value="Next" >
            </div>
          </div> 
      </form>-->
      </div>
    </div>      
  </div>
</div>


  


 